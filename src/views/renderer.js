/**
 * Processo de renderização
 * Tela Principal
 */

console.log('Processo de renderização')

function cliente(){
   // console.log('Teste do botão cliente')
   // Uso da=a api(autorizada no preload.js)
   api.clientWindow()
}


function os(){
    // console.log('Teste do botão OS')
    // Uso da=a api(autorizada no preload.js)
    api.osWindow()
}